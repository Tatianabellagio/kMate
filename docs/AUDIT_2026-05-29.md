# kMate cleanup audit — 2026-05-29

Post-reorg audit of the active tree (`src/ scripts/ panel/ benchmarks/ tests/
docs/`). Method: multi-agent sweep (8 subsystem auditors + adversarial
verification), then **every finding below was hand-re-verified against the
actual file contents** before being recorded here.

> **Caveat on the raw sweep.** The sandbox hit an intermittent fault where some
> agents' tool calls returned empty output. Several agents (and a few verifiers)
> consequently produced findings from missing or fabricated evidence. The raw
> run reported "23 confirmed"; after hand-verification, **2 of those are false
> positives** (caught below) and a few others were mis-scoped. Trust this file,
> not the raw counts.

Legend — verification: ✓ confirmed by me against the file · ⚠ plausible,
not yet hand-verified (confirm before changing) · ✗ false positive.
Auto-fix: a fix was applied in this pass · (none) = reported only.

---

## Applied in this pass (committed)

| ✓ | file | change |
|---|---|---|
| ✓ | `src/em_fixes/combined.py:36` | Removed `mu (mean m_b)` from the docstring's output-npz key list — `np.savez` (lines 169-173) never writes a `mu` key, so a consumer doing `d['mu']` would `KeyError`. Doc only. |
| ✓ | `src/block_haplotype_em.py:352` | Comment claimed the fallback `global_h` is a summed single aggregate EM; it is actually an *unweighted mean of converged per-block `h_founder`*. Corrected the comment. Code unchanged. |

Both are pure documentation/comment edits, zero behavior change.

---

## False positives caught (do NOT act on these)

- **✗ "src/README.md contradicts the code / docs/pipeline.md" (raw finding #3, HIGH).**
  Fabricated. `docs/pipeline.md` **does not exist**; `src/build_kmer_pa.py`
  **does** exist (10.8 KB, May 22); and the quoted README text
  ("build_kmer_pa.py — cn_matrix key", "all builders agree on cn_matrix/founder_ids")
  is **not in** `src/README.md`. The actual `src/README.md` is accurate. No change.
- **✗ "Archive per_sample_driver.py — nothing live uses it" (raw findings #4/#6, safe).**
  Wrong. `src/per_sample_driver.py` is referenced by **7 test scripts**
  (`run_seedmix_231/82_baseline`, `run_site04_array`, `run_per_sample_smoke`,
  `run_visor_test`, `test_genomewide_validation`, + deprecated `panel/imputation/*`).
  Moving/deleting it would break the test harness. See the real issue below.

---

## Confirmed — worth acting on (needs your call; not auto-applied)

### 1. ✓ `per_sample_driver.py` window mode is broken (HIGH, bug)
`src/per_sample_driver.py:162-166` calls
`project_blocks_to_records(h_blocks, status, global_h, var_pa, rec_block,
record_chrom=…, record_pos=…, blocks=…, smooth=True)` — the **pre-refactor
9-arg signature**. The current `src/block_em.py:252` is
`project_blocks_to_records(h_blocks, global_h, var_pa, record_block, var_called=None)`.
→ immediate `TypeError` (unexpected kwarg `record_chrom`).

- `--block-mode global` path (line 181+) is fine (`var_pa.T @ h`, never calls it).
- `--block-mode window` path **crashes**. And **two tests run this driver in
  window mode**: `tests/run_site04_array.sh:44` and `tests/run_visor_test.sh:58`.
  Those two are currently broken; the SEEDMIX tests use `global` and are fine.

**Decide:** (a) fix the call to the 5-arg signature (+ pass `var_called`), or
(b) repoint `run_site04_array.sh` / `run_visor_test.sh` at
`per_sample_per_chrom.py` (the documented production driver, whose window path is
correct at `per_sample_per_chrom.py:234`). Behavior-affecting → left for you.

### 2. ✓ `src/README.md` mislabels `per_sample_driver.py` as archived (doc-gap)
`src/README.md:55-56` lists `per_sample_driver.py` (and `batch_runner.py`,
`calibrate_alt_freqs.py`) under **"Archived — `archive/`"**, but the tracked file
lives in `src/` and is the active genome-wide test-harness driver (7 refs above).
Fix once #1 is decided: move it to an "active (test harness)" note and state that
its window path is broken / global-only — or actually archive it after migrating
the tests. (Tied to #1; doing them together avoids re-editing.)

### 3. ✓ `build_cn_from_pangenie.sh` points at a non-existent index prefix (panel, stale)
`panel/pangenie_genotyping/scripts/build_cn_from_pangenie.sh:34`
`INDEX_PREFIX=$BASE/data/pang69_pangenie_index  # produced by build_pangenie_index.sh`
— but `build_pangenie_index.sh:29` actually writes `pang_135_pangenie_index`, and
the v3qc builder writes `pang_v3qc_v2_pangenie_index`. **No script produces
`pang69_pangenie_index`**, so the script's sanity loop hard-fails. Likely
superseded by the arch3 `jobA5_build_cnvar.sh` path. **Decide:** repoint the
prefix (and the matching `REF`, see #4) or mark the script dead.

### 4. ⚠ `build_cn_from_pangenie.sh` REF may mismatch its index (panel, bug)
Same script line 35 uses `…/TAIR10.chr.fa` (non-iupacN) while the v3qc index
builder uses `TAIR10.chr.iupacN.fa`. If the kmers table/merged VCF were built
against iupacN, the kmer_pa matrix would be subtly wrong. Tied to #3; confirm which
REF the consumed index was built with before changing.

### 5. ✓ Untracked junk on disk (cleanup)
`src/._block_em.py`, `src/._em_solver.py`, `src/._per_sample_per_chrom.py`
(macOS AppleDouble) and `src/.ipynb_checkpoints/em_solver-checkpoint.py`. **Not
git-tracked**, so no history impact. Safe to `rm`. Left in place because they're
untracked (per your standing "don't touch untracked files" preference) — say the
word and I'll delete them and add `._*` / `.ipynb_checkpoints/` to `.gitignore`.

### 6. ✓ Untracked, stale em_fixes launchers (stale)
`src/em_fixes/run_correlation.sh:15` (`ROOT=…/hapfire_sv`, `…/poolfreq/…`) and
`src/em_fixes/run_invac.sbatch:11` (`REPO=/carnegie/nobackup/…/hapfire_sv/poolfreq/…`)
hardcode the pre-rename `hapfire_sv` + `poolfreq/` paths and would fail on
invocation. Both **untracked** (so the path-fix commits never saw them), and
`run_invac.sbatch` is a different-cluster (carnegie) wrapper. Left untouched per
the untracked rule. Fix = repoint `ROOT`/`REPO` to `…/kmate` and drop the
`poolfreq/` segment; then `git add` if you want them reproducible.

---

## Confirmed — lower priority (mostly text / organization)

- **⚠ `src/em_fixes/` has 6 near-duplicate weighted-Poisson EM solvers**
  (`combined.solve_em_weighted`, `perbubble.solve_em_weighted`,
  `invac.weighted_em`, `balancedbubble.solve_em`, `normalizer_factorial.em`,
  `correlation`), while `balanced.py` already imports the canonical
  `em_solver.solve_em` (which supports the `omega` weighting). Drift risk between
  copies. Refactor to one `weighted_solve_em(counts, kmer_pa, omega, normalizer)`.
  (medium, refactor — not auto-safe.)
- **⚠ block_haplotype_em uniform-1/N fallback bias** (`block_haplotype_em.py:407`):
  skipped/out-of-block records get AF projected through a flat `1/N` founder prior,
  pulling them toward the panel mean in selected pools. A chrom-level EM (or the
  already-computed `global_h`) would be better. *Off by default* — only on the
  skipped-block path. (medium, algorithm.)
- **⚠ smooth_h_across_blocks neighbor-by-index** (`block_haplotype_em.py:132`):
  picks left/right neighbors by integer block index, assuming index==genomic
  order. Production window mode pre-sorts blocks so it's **safe there**; only bites
  the archived `per_sample_bigld_haplotype.py` driver. Latent. (medium.)
- **⚠ define_windows trailing windows** (`block_em.py:65`): window *count* derived
  from `window_step` but each window spans `window_bp`; with overlap the last
  windows extend ~`window_bp` past chrom end. Harmless (empty windows excluded),
  mild work inflation. (low, perf.)
- **⚠ panel/arch3/chr1 has 17 `jobA1..F1` scripts with no README/DAG** and a
  `jobE2` vs `jobE2v2` ambiguity — order lives only in per-script header comments.
  Add `panel/arch3/chr1/README.md` with the job DAG and mark the stale E2.
  (doc-gap.)
- **⚠ Two competing 231-founder Chr1 panel build paths** (pangenie_genotyping
  `build_v3qc_v3_phase_a/b` vs arch3 `jobA2/jobA3` rebuild) with no signpost in
  `panel/README.md`. (confusing-pipeline.)
- **⚠ `build_v3qc_merged.sh:112`** echoes "records after MAC<2 filter" but Step 4
  now drops only `AC=0`. Text-only. (low.)
- **⚠ `build_pangenie_index.sh:14`** header says "from pang_69's VCF … job 56180"
  but consumes `pang_1001gplus_all.vcf.gz` and writes `pang_135_pangenie_index`.
  Text-only. (low.)
- **⚠ `panel/imputation/*` scripts hardcode the old `hapfire_sv/` path** — but the
  dir is already marked Deprecated in `panel/README.md`. Banner or delete. (low.)
- **⚠ `score_balanced.py:46` / `score_correlation.py:39` read a legacy
  `h_per_alpha` key** produced only by the archived `sweep_shape_norm_h_only.py`.
  Document provenance or regenerate. (low.)
- **⚠ em_fixes reproducibility gaps** — `run_whitening_all.py` tracked but
  `run_correlation.sh`/`run_invac.sbatch` not, and `combined/balancedbubble/
  perbubble` have no committed launcher. (low, organizational.)

---

## Good news (verified clean)

- `src/README.md` production recipe and the `global`/`window` estimator docs are
  accurate (the one "doc contradiction" finding was a false positive).
- All cross-cutting documentation-consistency findings from the sweep were
  rejected on verification — no widespread `hapfire_sv`/`poolfreq` leakage in the
  tracked active tree (only the two untracked em_fixes launchers above).
- `src/em_solver.py` / `block_em.py` core EM math reads correctly; no confirmed
  correctness bug in the default production path.
